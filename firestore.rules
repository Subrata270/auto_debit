
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all read, write, update, and delete operations
    // on all documents in all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
